spring.application.name=Auth
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/moneymap
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD} 
spring.datasource.driver-class-name=org.postgresql.Driver
# Make sure to change the current set password from DB_Password to DB_PASSWORD. setx DB_PASSWORD "your_password_here"